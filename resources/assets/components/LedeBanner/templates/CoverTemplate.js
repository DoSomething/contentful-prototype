import React from 'react';
import PropTypes from 'prop-types';

import './cover-template-lede-banner.scss';

const CoverTemplate = (props) => {
  console.log(props);

  return (
    <header role="banner" className="lede-banner">
      hello
    </header>
  );
};

export default CoverTemplate;
